# DataSource
spring.datasource.url=jdbc:postgresql://database:5432/dsi_dev_profiles
spring.datasource.username=DSi
spring.datasource.password=S3cr3T

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.hibernate.ddl-auto=none

# Liquibase
spring.liquibase.enabled=false
spring.liquibase.contexts=prod

github_client_id=<github_oauth_app_client_id>
github_client_secret=<github_oauth_app_client_secret>
